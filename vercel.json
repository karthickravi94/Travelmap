{
  "buildCommand": "prisma generate && if [ -n \"$POSTGRES_PRISMA_URL\" ] && [ \"$POSTGRES_PRISMA_URL\" != \"\" ]; then prisma db push --accept-data-loss && npx tsx prisma/seed.ts; fi && next build",
  "framework": "nextjs",
  "installCommand": "npm install"
}
